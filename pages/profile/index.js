// this is the path which the user will be directed to when they click on a user's provided profile link, this only provides the user a way to see the recipes a user has saved, rather than make changes.
import axios from 'axios'

export default function Profile() {
    return (
        <p>works</p>
    )
}